#lang racket
; (require racket/format)
(require "../lib/advent.rkt")

; (+ 1 1)
; (define input (open-input-file "input.txt"))
; (define words (string-split (read-line input)))
; (define nums (map (lambda (s) (string->number s)) words))
; nums
nums
